[pytest]
# Ignore the scripts folder which contains helper probes that start with test_ and
# would otherwise be collected by pytest. Keep test discovery focused on tests/.
norecursedirs = scripts
python_files = tests/test_*.py
